import { html } from "ui";

export const version = "v1.1.0.dev"; // TODO: Don't forget to remove the ".dev" suffix before release

export const build = 9; // NOTE: v1.1.0 build

export const info = html``; // TODO: Generate a new info from changelog for v1.1.0
